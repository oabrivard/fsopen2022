{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "fullstackopen part 3 phonebook",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../part2/phonebook && npm run build && cd ../../part3/phonebook && cp -r ../../part2/phonebook/build .",
    "deploy": "git push heroku master",
    "deploy:full": "git add . && git commit -m uibuild && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint ."
  },
  "author": "Olivier Abrivard",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.10",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.13.0",
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "^16.13.2"
  }
}
